{
  "name": "mode_rules",
  "group": "rules",
  "kind": "u16",
  "desc": "sparse mod layer of mode_rules: only the rules it sets, merged over the stock table in profile order (wfengine --sparsify)",
  "overrides": {
    "rumble_bell": 8,
    "rumble_live_cap": 8,
    "rumble_spawn_frames": 90,
    "select_extended": 1
  }
}
