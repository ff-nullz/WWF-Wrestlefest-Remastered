{
  "name": "tag_rules",
  "group": "rules",
  "kind": "u16",
  "desc": "sparse mod layer of tag_rules: only the rules it sets, merged over the stock table in profile order (wfengine --sparsify)",
  "overrides": {
    "runin_grace_frames": 9999
  }
}
